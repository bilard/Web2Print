{
  "name": "web2print-indesign-plugin",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/panel.ts --bundle --format=iife --external:indesign --external:uxp --outfile=dist/panel.js",
    "test": "vitest run"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "vitest": "^4.1.7",
    "typescript": "^5.6.3"
  }
}
